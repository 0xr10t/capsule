{
  "name": "@capsule/proof-engine-rust",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cargo build",
    "test": "cargo test",
    "wasm:node": "wasm-pack build --target nodejs --out-dir pkg-node",
    "wasm:web": "wasm-pack build --target web --out-dir pkg-web"
  }
}

